/// The result of a thumb gesture in [LeitnerScreen].
///
/// [up] means the card was answered correctly — level increases.
/// [down] means wrong — card resets to level 0.
enum LevelDirection { up, down }
